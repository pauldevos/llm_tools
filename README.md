# Adding Jupyter Kernels to Menu Option
```poetry run python -m ipykernel install --user --name pymedphys```

### Poetry Kernel within Jupyter (not sure if/when needed)
- https://github.com/pathbird/poetry-kernel
